# requirements-standalone.txt
# Dùng cho conda env trên server (KHÔNG Docker)
# conda create -n asr python=3.10 && conda activate asr

# Core audio
soundfile
scipy
librosa

# Whisper (CTranslate2)
faster-whisper

# Diarization
pyannote.audio==4.0.4

# Diarization runtime deps (no-deps install above, these fill in gaps)
asteroid-filterbanks>=0.4.0
einops>=0.8.1
huggingface-hub>=0.28.1
lightning>=2.4
matplotlib>=3.10.0
pyannote-core>=6.0.1
pyannote-database>=6.1.1
pyannote-metrics>=4.0.0
pyannote-pipeline>=4.0.0
pytorch-metric-learning>=2.8.1
rich>=13.9.4
safetensors>=0.5.2
torch-audiomentations>=0.12.0
torchmetrics>=1.6.1

# Torch (install separately — version depends on CUDA):
# CUDA 12.x:  pip install torch==2.5.1+cu121 torchaudio==2.5.1+cu121 --index-url https://download.pytorch.org/whl/cu121
# CUDA 11.8:  pip install torch==2.5.1+cu118 torchaudio==2.5.1+cu118 --index-url https://download.pytorch.org/whl/cu118
